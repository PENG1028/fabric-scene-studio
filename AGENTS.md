# Repository instructions

Read skills/fabric-scene-studio/SKILL.md. Resolve paths from this repository root. Keep ComfyUI nodes, v7 template, portable runner and Skill consistent; they are not automatically synchronized.

Preserve user edits and node modes. Keep credentials, customer photos, local paths and logs/caches out of git. Installation targets an explicitly selected ComfyUI root and does not restart or generate. Public examples require user-provided photos and verified captions.

Follow session authorization for paid generation; single-image requests must not queue every scene. Never auto-retry ambiguous requests. Evaluate material accuracy, wearability and art direction separately. Offline tests require no real API key.
